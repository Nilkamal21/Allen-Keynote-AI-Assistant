services:
  - type: web
    name: allens-keynotes-ai
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: COHERE_API_KEY
        sync: false
